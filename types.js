/**
 * DeviceType constants for categorizing smart home hardware.
 * Converted from TypeScript enum to a frozen object.
 */
export const DeviceType = {
  LIGHT: 'LIGHT',
  AC: 'AC',
  CAMERA: 'CAMERA',
  THERMOSTAT: 'THERMOSTAT',
};

/**
 * SceneType constants for different dashboard modes.
 * Converted from TypeScript enum to a frozen object.
 */
export const SceneType = {
  AWAY: 'Away',
  SLEEP: 'Sleep',
  MOVIE: 'Movie Night',
  HOME: 'Home',
};

// Note: Interfaces (Device, SecurityLog, EnergyData) have been removed 
// as they are TypeScript-only structures.
